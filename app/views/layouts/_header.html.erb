<header class="z-50 flex h-16 border-b border-gray-900/10">
  <div class="mx-auto flex w-full max-w-7xl items-center justify-between px-4 sm:px-6 lg:px-8">
    <div class="flex flex-1 items-center gap-x-6">
      <button type="button" class="-m-3 p-3 md:hidden">
        <span class="sr-only">Open main menu</span>
        <svg class="size-5 text-gray-900" viewBox="0 0 20 20" fill="currentColor" aria-hidden="true" data-slot="icon">
          <path fill-rule="evenodd" d="M2 4.75A.75.75 0 0 1 2.75 4h14.5a.75.75 0 0 1 0 1.5H2.75A.75.75 0 0 1 2 4.75ZM2 10a.75.75 0 0 1 .75-.75h14.5a.75.75 0 0 1 0 1.5H2.75A.75.75 0 0 1 2 10Zm0 5.25a.75.75 0 0 1 .75-.75h14.5a.75.75 0 0 1 0 1.5H2.75a.75.75 0 0 1-.75-.75Z" clip-rule="evenodd" />
        </svg>
      </button>
      <img class="h-8 w-auto" src="https://tailwindui.com/plus/img/logos/mark.svg?color=indigo&shade=600" alt="Your Company">

      <% if user_signed_in? && current_user.multi_account? %>
        <%= dropdown_component do |dropdown| %>
          <% dropdown.with_button do %>
            <div class="flex items-center gap-4">
              <%= avatar_component(account: Current.account) %>
              <%= Current.account.name %>
            </div>
          <% end %>

          <% current_user.accounts.each do |account| %>
            <% dropdown.with_item(href: account_switch_path(account, return_to: request.path), data: { turbo_method: :patch }) do %>
              <div class="flex items-center gap-4">
                <%= avatar_component(account: account) %>
                <%= account.name %>
              </div>
            <% end %>
          <% end %>
        <% end %>
      <% end %>
    </div>
    <nav class="md:gap-x-11 md:text-sm/6 md:font-semibold md:text-gray-700">
      <%# Nav links go here %>
      <%# <a href="#">Home</a> %>
    </nav>

    <% if user_signed_in? %>
      <%= dropdown_component(align: :right) do |dropdown| %>
        <% dropdown.with_button do %>
          <%= avatar_component(user: current_user) %>
        <% end %>

        <% dropdown.with_item(href: edit_user_registration_path) do %>
          Profile
        <% end %>
        <% dropdown.with_item(href: edit_accounts_password_path) do %>
          Change password
        <% end %>
        <% dropdown.with_item(href: accounts_path) do %>
          Accounts
        <% end %>
        <% dropdown.with_item(href: destroy_user_session_path, data: { turbo_method: 'delete' }) do %>
          Log out
        <% end %>
      <% end %>
    <% else %>
      <%= link_to new_user_session_path, class: 'text-sm/6 font-semibold text-gray-900' do %>
        Log in <span aria-hidden="true">&rarr;</span>
      <% end %>
    <% end %>
  </div>
</header>
