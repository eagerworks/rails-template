<!DOCTYPE html>
<html>
  <%= render 'layouts/head' %>

  <body class="bg-gray-50">
    <div class="min-h-screen">
      <%= render 'layouts/flash_messages' %>

      <%= yield %>
    </div>
  </body>
</html>
